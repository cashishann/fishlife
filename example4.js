var x= 50;
var y= 50;
var xSpeed=10;
var ySpeed=10                                                                                                                                                             ;   
var bg;                                                                                                                 
function setup() {
	createCanvas (700, 500);
	bg= loadImage("download.png");
}
	
function draw(){
	background (bg);
	ellipse(x,y,70,70);
	x= x+xSpeed;
	y=y+ySpeed;
if (y>height || y<0){
	ySpeed=ySpeed*-1;
}
if (x>width || x<0){
	xSpeed=xSpeed*-1;




































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































































}
}
